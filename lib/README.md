# Library Dependencies

This directory contains Git submodules:

- **libDaisy** — Electro-Smith hardware abstraction library
- **DaisySP** — Electro-Smith DSP library

These are populated by `git clone --recursive` or `git submodule update --init --recursive`.
